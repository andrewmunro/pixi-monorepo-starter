# pixi-monorepo

Simple example monorepo for running multiple pixi.js apps with React.

### Setup

```bash
bun install
```

### Development

1. Compile packages
    ```bash
    bun dev
    ```
2. Run the viewer
    ```bash
    cd viewer
    bun dev
    ```
